import React from "react";
import { Typography } from "@material-ui/core";

const Header = () => {
	return (
		<Typography align="center" style={{ width: "90%", margin: "0 auto" }} variant="h1">
			Welcome, let’s pick a key and start playing!
		</Typography>
	);
};

export default Header;
